server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/banco
    username: postgres
    password: admin123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

rabbitmq:
  host: localhost
  port: 5672